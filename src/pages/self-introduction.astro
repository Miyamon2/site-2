---
import Layout from "../components/layout.astro";
import Header from "../components/Header.astro";
import TwitterTimeline from "../components/TwitterTimeline.astro";
import WorksSection from "../components/WorksSection.astro";
---

<Layout>
    <main>
        <!-- 背景画像を動画の前に表示 -->
        <img
            src="./miyamonのsite背景画像.png"
            alt="miyamonのサイト背景画像"
            class="background-image"
        />
        <video autoplay loop muted playsinline>
            <source
                src="https://github.com/Miyamon2/site-2/raw/refs/heads/master/public/miyamon%E3%81%AEsite%E8%83%8C%E6%99%AF.mp4"
                type="video/mp4"
            />
        </video>
        <Header currentPage="self-introduction" />
        <div class="container">
            <section>
                <h2>自己紹介</h2>
                <p>
                    こんにちは！チーム逸般人のMiyamonと申します。Minecraftが得意で教育福祉分野やガジェット、プログラミング、動画編集にも興味があります。今は子供たちにマイクラを教えたり、自分のサイトを作ったりしています。就労移行支援にいき動画編集の仕事をするために日々訓練をしています
                </p>
            </section>

            <section>
                <h2>趣味</h2>
                <p>
                    ガジェットを集めることやPVPのゲームをする、アニメ視聴、ネットニュースを見る、ファッションも好きでなぜかおじさんを目指してます。
                </p>
            </section>

            <section class="skills-section">
                <h2>スキル</h2>
                <div class="skills">
                    <div class="skill-card">
                        <div class="skill-icon">🎮</div>
                        <span class="skill-name">Minecraft</span>
                    </div>
                    <div class="skill-card">
                        <div class="skill-icon">💻</div>
                        <span class="skill-name">パソコン組み立て</span>
                    </div>
                    <div class="skill-card">
                        <div class="skill-icon">🎯</div>
                        <span class="skill-name">プレゼンテーション</span>
                    </div>
                    <div class="skill-card">
                        <div class="skill-icon">📱</div>
                        <span class="skill-name">HTML</span>
                    </div>
                    <div class="skill-card">
                        <div class="skill-icon">🎮</div>
                        <span class="skill-name">PVPゲーム</span>
                    </div>
                    <div class="skill-card">
                        <div class="skill-icon">🖼️</div>
                        <span class="skill-name">絵を描く</span>
                    </div>
                    <div class="skill-card">
                        <div class="skill-icon">🎨</div>
                        <span class="skill-name">Adobe illustrator</span>
                    </div>
                    <div class="skill-card">
                        <div class="skill-icon">🖥️</div>
                        <span class="skill-name">Adobe Photoshop</span>
                    </div>
                    <div class="skill-card">
                        <div class="skill-icon">🎥</div>
                        <span class="skill-name">Adobe Premiere Pro</span>
                    </div>
                </div>

                <section>
                    <h2>経歴</h2>
                    <ul>
                        <li>2006年 - 誕生</li>
                        <li>2018年 - 桜丘中学校入学</li>
                        <li>
                            2019年 -
                            学校にお金を出してもらってパソコンを作り学校関係者の前でプレゼンする
                        </li>
                        <li>
                            2020年 -<a
                            href="https://www.city.setagaya.lg.jp/01300/1397.html">
                            学校にホットルームを作ってもらえるようにプレゼンしてそれが実際に通る    
                        </a>
                        </li>
                        <li>2021年 - 高校に入学</li>
                        <li>
                            2021年 - <a
                                href="https://minecraftcup.com/works2021/workarticle2021/?work_number=306"
                                >チーム逸般人としてマイクラカップでインプレスこどもとIT賞を取る</a
                            >
                        </li>
                        <li>
                            2022年 - <a href="https://ideaworkshop2.peatix.com/"
                                >idea active
                                japanのイベントをチーム逸般人としてマイクロソフト本社で開催</a
                            >
                        </li>
                        <li>2023年 - オンライン場所colorのボランティア活動</li>
                        <li>2024年 - html勉強し自分のsiteをつくる</li>
                        <li>
                            2025年 -
                            動画編集の仕事に就職するために就労移行支援に行っている
                        </li>
                    </ul>
                </section>

                <section>
                    <h2>目標</h2>
                    <p>
                        将来的には、動画編集を職にして、副職で子供たちに動画編集やマインクラフトを教えられたりできればと思っています。
                    </p>
                </section>

                <WorksSection />
            </section>
        </div>
    </main>

    <style>
        /* アニメーション定義 */
        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* 基本スタイル */
        body {
            font-family: "Arial", sans-serif;
            line-height: 1.6;
            margin: 0;
            padding: 0;
            color: #e0e0e0;
            background-color: #000;
        }

        /* 背景とメイン要素 */
        .background-image {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            object-fit: cover;
            z-index: -2;
        }

        main {
            margin-inline: 2rem;
            overflow: hidden;
            position: relative;
            min-height: 100vh;
        }

        main video {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            object-fit: cover;
            z-index: -1;
        }

        main::after {
            content: "";
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.7));
            z-index: -1;
        }

        /* コンテナ */
        .container {
            max-width: 1440px;
            margin: 1rem auto ;
            padding: 1.5rem 3.2rem;
            background: linear-gradient(
                135deg,
                rgba(18, 18, 18, 0.8),
                rgba(0, 0, 0, 0.7)
            );
            backdrop-filter: blur(12px);
            border-radius: 20px;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.4);
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        /* セクション */
        section {
            margin-bottom: 3.5rem;
            animation: fadeIn 0.8s ease-out forwards;
            opacity: 0;
            position: relative;
        }

        section:nth-child(2) {
            animation-delay: 0.3s;
        }
        section:nth-child(3) {
            animation-delay: 0.6s;
        }
        section:nth-child(4) {
            animation-delay: 0.9s;
        }
        section:nth-child(5) {
            animation-delay: 1.2s;
        }

        /* 見出し */
        h2 {
            color: #64b5f6;
            font-size: 1.8rem;
            margin-bottom: 1.5rem;
            position: relative;
            padding-left: 1rem;
        }

        h2::before {
            content: "";
            position: absolute;
            left: 0;
            top: 50%;
            transform: translateY(-50%);
            width: 4px;
            height: 70%;
            background: linear-gradient(to bottom, #64b5f6, #81c784);
            border-radius: 2px;
        }

        /* スキルセクション */

        .skills {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1.5rem;
            margin-top: 2rem;
        }

        .skill-card {
            background: linear-gradient(
                135deg,
                rgba(100, 181, 246, 0.1),
                rgba(129, 199, 132, 0.1)
            );
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 15px;
            padding: 1.5rem;
            text-align: center;
            transition: all 0.3s ease;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 0.1rem;
            backdrop-filter: blur(5px);
        }

        .skill-card:hover {
            transform: translateY(-5px);
            background: linear-gradient(
                135deg,
                rgba(100, 181, 246, 0.2),
                rgba(129, 199, 132, 0.2)
            );
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
        }

        .skill-icon {
            font-size: 3rem;
            margin-bottom: 0.5rem;
        }

        .skill-name {
            font-size: 1.2rem;
            font-weight: bold;
            color: #fff;
            margin: 0.5rem 0;
        }

        /* リスト */
        ul {
            list-style: none;
            padding: 0;
            position: relative;
        }

        ul::before {
            content: "";
            position: absolute;
            left: 0;
            top: 0;
            width: 2px;
            height: 100%;
            background: linear-gradient(to bottom, #3498db, #2ecc71);
        }

        li {
            position: relative;
            padding-left: 2rem;
            margin-bottom: 1.5rem;
        }

        li::before {
            content: "";
            position: absolute;
            left: -5px;
            top: 50%;
            transform: translateY(-50%);
            width: 12px;
            height: 12px;
            background: #3498db;
            border-radius: 50%;
            box-shadow: 0 0 0 4px rgba(52, 152, 219, 0.2);
        }

        /* リンク */
        a {
            color: #64b5f6;
            text-decoration: none;
            transition: color 0.3s ease;
        }

        a:hover {
            color: #ffffff;
        }

        /* レスポンシブデザイン */
        @media (max-width: 1024px) {
            .container {
                margin: 1.5rem;
                padding: 2rem;
            }

            .skills {
                grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            }
        }

        @media (max-width: 768px) {
            main {
                margin-inline: 1rem;
            }

            .container {
                margin: 1rem;
                padding: 1.5rem;
            }

            .skills {
                grid-template-columns: repeat(auto-fit, minmax(130px, 1fr));
                gap: 1rem;
            }

            .skill-card {
                padding: 1.2rem;
            }

            .skill-icon {
                font-size: 1.8rem;
            }

            .skill-name {
                font-size: 1rem;
            }

            section {
                margin-bottom: 2.5rem;
            }

            h2 {
                font-size: 1.6rem;
                margin-bottom: 1.2rem;
            }

            p,
            li {
                font-size: 0.95rem;
                line-height: 1.5;
            }
        }

        @media (max-width: 480px) {
            main {
                margin-inline: 0.25rem;
            }
            .container {
                margin: 0.5rem;
                padding: 1rem;
            }

            .skills {
                grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
                text-wrap: balance;
            }

            .skill-card {
                padding: 0.8rem;
            }

            .skill-icon {
                font-size: 3rem;
            }

            .skill-name {
                font-size: 1.2rem;
            }

            section {
                margin-bottom: 2rem;
            }

            h2 {
                font-size: 1.4rem;
            }
        }
    </style>

    <style>
        /* ロゴの表示設定 */
        .logo-full {
            display: inline-block; /* PCでは表示 */
        }

        .logo-mini {
            display: none; /* PCでは非表示 */
        }

        @media (max-width: 768px) {
            /* スマホ表示時のロゴ切り替え */
            .logo-full {
                display: none; /* スマホでは非表示 */
            }

            .logo-mini {
                display: inline-block; /* スマホでは表示 */
            }
        }

        /* Twitterタイムラインのスタイル */
        .twitter-timeline-container {
            margin: 1rem 0;
            max-width: 100%;
            overflow: hidden;
            border-radius: 12px;
            background-color: rgba(0, 0, 0, 0.2);
            backdrop-filter: blur(10px);
            padding: 1rem;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        /* 作品セクションのスタイル */
        .works-section {
            padding: 2rem;
            background: rgba(0, 0, 0, 0.2);
            border-radius: 15px;
            backdrop-filter: blur(10px);
        }

        .works-gallery {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            max-width: 100%;
            gap: 2rem;
            margin-top: 2rem;
        }

        /* レスポンシブ対応で横3つまでに制限 */
        @media (min-width: 1200px) {
            .works-gallery {
                grid-template-columns: repeat(3, 1fr);
            }
        }

        @media (min-width: 768px) and (max-width: 1199px) {
            .works-gallery {
                grid-template-columns: repeat(2, 1fr);
            }
        }

        @media (max-width: 767px) {
            .works-gallery {
                grid-template-columns: 1fr;
            }
        }

        .work-item {
            background: linear-gradient(
                135deg,
                rgba(100, 181, 246, 0.1),
                rgba(129, 199, 132, 0.1)
            );
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 15px;
            padding: 1.5rem;
            text-align: center;
            transition: all 0.3s ease;
            backdrop-filter: blur(5px);
        }

        .work-item:hover {
            transform: translateY(-5px);
            background: linear-gradient(
                135deg,
                rgba(100, 181, 246, 0.2),
                rgba(129, 199, 132, 0.2)
            );
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
        }

        .work-item img {
            width: 100%;
            height: 250px;
            object-fit: contain;
            border-radius: 10px;
            margin-bottom: 1rem;
            background-color: rgba(255, 255, 255, 0.05);
        }

        .work-title {
            color: #64b5f6;
            font-size: 1.3rem;
            font-weight: bold;
            margin: 1rem 0 0.5rem 0;
        }

        .work-description {
            color: #b0b0b0;
            font-size: 0.9rem;
            line-height: 1.4;
            margin: 0;
        }

        @media (max-width: 768px) {
            .works-gallery {
                grid-template-columns: 1fr;
                gap: 1.5rem;
            }

            .work-item img {
                height: 150px;
            }

            .work-title {
                font-size: 1.1rem;
            }
        }
    </style>
</Layout>
